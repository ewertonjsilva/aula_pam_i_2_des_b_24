import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        borderWidth: 1, 
        padding: 10,      
        width: 300, 
        marginBottom: 8,   
    },  
    mensagem: {
        fontSize: 20, 
    },
});

export default styles;